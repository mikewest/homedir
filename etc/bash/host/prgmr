# ~/etc/bash/host/prgmr
# -*- Mode: Bash; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 2 -*-
#
# commands specific to my prgmr vps

export VISUAL=vim;

function gd {
    git diff "$@" | vim -
}

export prompt_prefix="${pyellow}prgmr${preset} "
